{
  "type": "module",
  "dependencies": {
    "discord.js": "^14.26.4",
    "dotenv": "^17.4.2",
    "typescript": "^6.0.3",
    "zod": "^4.4.3"
  },
  "overrides": {
    "undici": "6.27.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "ts-node": "^10.9.2",
    "vitest": "^4.1.10"
  },
  "scripts": {
    "compile": "tsc -p .",
    "predev": "rm channelTable.json && npm run compile",
    "dev": "node --env-file=.env.dev dist/main.js",
    "premock": "npm run compile",
    "mock": "node --env-file=.env.dev dist/mockServer.js",
    "prestart": "npm run compile",
    "start": "node --env-file=.env dist/main.js"
  }
}